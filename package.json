{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "test:worker": "cd apps/services/worker && python -m pytest tests"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "apps/services/*",
    "packages/*"
  ],
  "dependencies": {
    "nestjs-zod": "^5.4.0"
  }
}
