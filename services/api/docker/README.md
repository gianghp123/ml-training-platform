# docker/

**Purpose:** Docker Compose configuration for local development services.

**Default:** PostgreSQL 15. Add more services (Redis, Elasticsearch, etc.) as needed.

**Usage:** `make up` to start, `make down` to stop.
